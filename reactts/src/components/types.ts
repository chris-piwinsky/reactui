// types.ts

export interface TableRow {
    id: number;
    first_name: string;
    last_name: string;
    email: string;
    address: any; // Add the correct type for address
    employment: any; // Add the correct type for employment
    // Other fields as needed
  }
  